To open this project, Qt 5.15.0 should be installed. And to execute, rigt click on WordMatching.pro file and open it with QT application. Once Qt opened there is project selectin part on the left down part of Qt and select WordMatching. And then click "Run qmake" on "Build" tab on the left upper part of QT. After all that press run which is green play buyyon below project selection button or simply press Ctrl+R. 
